apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolo-backend
spec:
  selector:
    matchLabels:
      app: yolo-backend
  template:
    metadata:
      labels:
        app: yolo-backend
    spec:
      containers:
      - name: yolo-backend